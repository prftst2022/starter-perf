import React from "react"
import styles from "./SomeHeader.module.scss"

const SomeHeader = () => {
  return <div className={styles["some-header"]}>Hover to change color.</div>
}

export default SomeHeader
